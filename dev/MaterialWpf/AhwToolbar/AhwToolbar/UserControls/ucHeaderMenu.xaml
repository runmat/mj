<UserControl x:Class="AhwToolbar.UserControls.UcHeaderMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <DockPanel>
<!--        <Button Command="{Binding TestCommand}">...</Button>-->
        <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" Visibility="Hidden" IsChecked="False" x:Name="MenuToggleButton"/>
        <materialDesign:PopupBox DockPanel.Dock="Right" PlacementMode="BottomAndAlignRightEdges" StaysOpen="False">
            <StackPanel>
                <Button Content="Beenden" Click="OnCloseClick" />
            </StackPanel>
        </materialDesign:PopupBox>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">Ahw Toolbar</TextBlock>
    </DockPanel>

</UserControl>
