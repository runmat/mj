<UserControl x:Class="CarDocu.UserControls.DocuGroup.ucPdfFolderRibbonButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fl="clr-namespace:Fluent;assembly=Fluent" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <fl:Button Command="{Binding ScanPdfFolderOpenCommand}" LargeIcon="{StaticResource image/32x32/pdf_folder}" 
                               Header="PDF Ordner öffnen">
        <fl:Button.ToolTip>
            <fl:ScreenTip DataContext="{Binding PlacementTarget, RelativeSource={RelativeSource Self}}" Title="{Binding Header}"   
                                          Text="Öffnet den PDF Ausgabe Ordner im Windows Explorer."
                                />
        </fl:Button.ToolTip>
    </fl:Button>
</UserControl>
