<UserControl x:Class="CarDocu.UserControls.AllGroups.ucDomainLocation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>

        <StackPanel Orientation="Horizontal" Margin="27,2,0,0" VerticalAlignment="Top">
            <ContentPresenter Content="{StaticResource image/16x16/location}" ToolTip="Aktueller Standort" Margin="0,1,5,0" VerticalAlignment="Top" />
            <Button Command="{Binding LocationSelectionToggleCommand}" Style="{StaticResource HyperLinkSmall}" Content="{Binding LogonUser.DomainLocationFriendlyName}" ToolTip="Aktueller Standort. Klicken um Standort zu ändern!" 
                          Foreground="#303030"  FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,1,0,0"  />
        </StackPanel>

    </StackPanel>
</UserControl>
