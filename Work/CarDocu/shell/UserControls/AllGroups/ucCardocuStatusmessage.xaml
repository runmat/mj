<UserControl x:Class="CarDocu.UserControls.AllGroups.ucCardocuStatusmessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
            <Image Source="{Binding StatusmessageIconSource}" Width="16" Height="16" ToolTip="{Binding StatusmessageTip}" Margin="0,0,0,0"  />
            <TextBlock Text="{Binding Statusmessage}" Margin="5,0,0,0" FontWeight="Normal" />
        </StackPanel>

    </StackPanel>
</UserControl>
