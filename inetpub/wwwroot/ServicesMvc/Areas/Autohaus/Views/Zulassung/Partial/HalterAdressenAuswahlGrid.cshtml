@using CkgDomainLogic.DomainCommon.Models
@using GeneralTools.Resources
@{
    var gridName = "HalterAdressenAuswahlGrid";
}

<h5 class="margin-bottom-25 bold">@Localize.SelectCarOwnerAddress: </h5> 

@(Html.Telerik()
    .Grid<Adresse>()
    .Name(gridName)
    .XAjaxDataBinding("HalterAdressenAjaxBinding", "Zulassung")
    .Columns(columns =>
    {
        columns.Bound(c => c.ID).Title("").Filterable(false).Sortable(false)
            .ClientTemplate(
            "<button onclick='GridHalterAdresseOnSelect(\"<#= data.KundenNr #>\");' class='btn mini gray tooltips' data-original-title='" + CommonResources.PickAddress + "' data-placement='right'><i class='halflings-icon white chevron-right'></i></button>"
            );
        columns.Bound(c => c.Name1);
        columns.Bound(c => c.Name2);
        columns.Bound(c => c.StrasseHausNr);
        columns.Bound(c => c.PLZ);
        columns.Bound(c => c.Ort);
        columns.Bound(c => c.Land);
        columns.Bound(c => c.Iban);
    })
    .XAutoColumnConfiguration()
    .ClientEvents(events => events.XAutoClientEvents(gridName).OnComplete("OnComplete_AdressenAuswahlGrid"))
    .XPageSize(10)
    .XSort(sortOrder => sortOrder.Add(c => c.Name1).Ascending())
    .ToolBar(commands =>
    {
        commands.FilteredDataCommand("HalterAdressenAuswahlExportFilteredExcel", "Zulassung");
        commands.FilteredDataCommand("HalterAdressenAuswahlExportFilteredPDF", "Zulassung");
    })
)
