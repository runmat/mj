@model CkgDomainLogic.Archive.Models.EasyAccessResultField

<div class="no-break-container margin-top-5">
    <span class="help-inline left">@Model.Name:</span>
    @Html.HiddenFor(m => Model.Id)
    @Html.TextBoxFor(m => Model.FieldValue, new { @class = "m-wrap small margin-right-20" })
</div>