@using CkgDomainLogic.Equi.ViewModels
@model DokumenteOhneDatenViewModel

<h3 class="page-title" id="headerTitle">@Localize.Equi_DokumenteOhneDaten <small></small></h3>

@section LayoutScript { _controlGroupValidationType = "individual"; }

<div id="divDokumenteOhneDatenGrid">
    @Html.Partial("DokumenteOhneDaten/DokumenteOhneDatenGrid", Model)
</div>

<div id="divGridItemDetails"></div>

<script type="text/javascript">

    var _modelIsValid = false;

    var ladeHinweis = '@Localize.LoadingData';

    function FormPreparePrivate() {
        GridAllColumnFilterApplyToGrid("GridDokumenteOhneDaten");
    }

    function FormPreparePrivateAjax() {
        FormPrepareAjax();
    }

    function GetGrid() {
        return $("#GridDokumenteOhneDaten").data("tGrid");
    }

    function GridLoadingHide() {
        // needed for chrome browser: Explicitely hide grids loading icon on multiple subsequent grid ajax requests (because of filter control initializing)
        setTimeout('$("#GridDokumenteOhneDatenItems .t-grid-pager .t-status .t-icon").removeClass("t-loading")', 500);
    }

    function GridRefresh() {
        GetGrid().ajaxRequest();
    }

    function OnDataBound_GridDokumenteOhneDaten() {
        FilteredData_Grid_OnDataBound($(this));
        $('.tooltips').tooltip();
    }

    function OnLoad_GridDokumenteOhneDaten() {
        $('.t-no-data td').text(ladeHinweis);
    }

    function OnColumnShowHide_GridDokumenteOhneDaten() {
        FilteredData_Grid_OnColumnShowHide($(this));
    }

    function OnColumnReorder_GridDokumenteOhneDaten() {
        FilteredData_Grid_OnColumnReorder($(this));
    }

    function ItemEdit(fin) {

        $.ajax(
        {
            type: "POST",
            url: "ItemEdit",
            data: { fin: fin },
            loadingShow: true,
            success: function (result) {
                ShowItemDetails(result);                
            },
            error: function (result) {
                alert(result.data);
            }
        });
    }

    function ShowItemDetails(result) {
        $("#divGridItemDetails").html(result);
        GridItemDetailsFormPreparePrivateAjax();
        $("#divDokumenteOhneDatenGrid").slideUp(function () {
            $("#divGridItemDetails").show();
        });
        scrollToTop();
    }

    function CloseItemDetails() {
        CloseItemDetailsInner();      
        FilterGrid("GridDokumenteOhneDaten");
    }

    function CloseItemDetailsInner() {
        $("#divGridItemDetails").slideUp(function () {
            $("#divDokumenteOhneDatenGrid").delay(200).show();
            window.scrollTo(0, 0);
        });
    }

    function GridItemDetailsFormPreparePrivateAjax() {
        FormPrepareAjax();

        LoadingHide();
        GridLoadingHide();

        $("button[type='submit']").click(function () {
            LoadingShow();
        });

        if (_modelIsValid)
            CloseItemDetails();
    }

    function DokumenteOhneDatenAjaxFormComplete() {
        FormPrepareAjax();
        if (_modelIsValid)
            CloseItemDetails();
    }

</script>
