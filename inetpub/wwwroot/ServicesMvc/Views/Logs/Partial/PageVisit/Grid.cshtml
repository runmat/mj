@model CkgDomainLogic.Logs.ViewModels.LogsViewModel
@using CkgDomainLogic.Logs.Models
@{
    var gridName = "GridLogsPageVisits";
}


@(Html.Telerik()
    .Grid<PageVisitLogItem>()
    .Name(gridName)

    .XAjaxDataBinding("LogsPageVisitsAjaxBinding", "Logs")
    .Columns(columns => columns.XBoundAll())
    .XAutoColumnConfiguration()
    .ClientEvents(events => events.XAutoClientEvents(gridName))
    .XPageSize(10)
    .XSort(sortOrder => sortOrder.Add(c => c.AppID))
    .ToolBar(commands =>
    {
        commands.FilteredDataCommand("ExportPageVisitLogItemsFilteredExcel", "Logs");
        commands.FilteredDataCommand("ExportPageVisitLogItemsFilteredPDF", "Logs");
    })
    .Groupable(g => g.Enabled(false)))

