@using CkgDomainLogic.Fahrer.Models
@{
    var gridName = "GridFreieAuftraege";
}

@(Html.Telerik()
    .Grid<FahrerAuftrag>()
    .Name(gridName)

    .XAjaxDataBinding("FreieAuftraegeAjaxBinding", "Fahrer")
    .Columns(columns =>
    {
        columns.Bound(c => c.WunschLieferDatum);
        columns.Bound(c => c.PlzOrtStart);
        columns.Bound(c => c.PlzOrtZiel);
        columns.Bound(c => c.PlzOrtRueck);
    }

    )
    .XAutoColumnConfiguration()
    .ClientEvents(events => events.XAutoClientEvents(gridName))
    .XPageSize(10)
    .XSort(sortOrder => sortOrder.Add(c => c.WunschLieferDatum).Ascending())
    .XToolBar("Fahrer")
    .Groupable(g => g.Enabled(false))
)

