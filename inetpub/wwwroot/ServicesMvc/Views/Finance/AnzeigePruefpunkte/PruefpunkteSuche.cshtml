@using CkgDomainLogic.Finance.Models
@using MvcTools.Models
@model PruefpunktSuchparameter

<div id="divPruefpunkteSuche">
    @using (Ajax.BeginForm("LoadPruefpunkte", "Finance", null,
        new MvcAjaxOptions("divPruefpunkteSuche") { OnComplete = "ShowPruefpunkte();" }))
    {    
        <div class="form-horizontal label-width-200" id="searchForm">
            @Html.ValidationSummary()
            
            @Html.FormTextBoxFor(m => m.Kontonummer, new { @class = "m-wrap", @onkeypress = "return NumbersOnly(false);" })
            @Html.FormTextBoxFor(m => m.PAID, new { @class = "m-wrap", @onkeypress = "return NumbersOnly(false);" })

            @Html.FormDateRangePickerFor(m => m.DatumRange, new { @class = "m-wrap" })

            @Html.FormCheckBoxFor(m => m.NurKlaerfaelle, new { @class = "m-wrap" })
           
            <div class="space20"></div>

            <div class="form-actions">
                <button type="submit" class='btn blue margin-left-40'><i class='icon-search'></i>&nbsp;Suchen</button>
            </div>
        </div>  
        
        <script type="text/javascript">
            _modelStateValid = '@ViewContext.ViewData.ModelState.IsValid.ToString().ToLower()';
        </script>
    }
</div>

<script type="text/javascript">

    function ShowPruefpunkte() {
        FormPreparePrivateAjax();

        $.ajax({
            type: "POST",
            url: "ShowPruefpunkte",
            success: function (result) {
                if (_modelStateValid == 'true') {
                    ShowPruefpunkteSearchResult(result);
                }
            }
        });
    }

</script>